#!/bin/bash
#
# This script creates a build bundle for the current project. It packages the necessary docker compose files
# and the build script for the .env file into a tar.gz archive. The resulting archive can be used to set up 
# the project on another machine without needing to clone the entire repository.
#