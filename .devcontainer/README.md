# Devcontainer Environments

This folder contains containerized workspace definitions.

- `development`: convenience-first environment for daily engineering work.
- `test`: lean validation environment for production-like checks.

Container definitions here support local development only. Production artifacts live under `deploy/`.
